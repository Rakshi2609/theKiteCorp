// Example GSAP helper functions
// You can add more GSAP-related utilities here
export function animateTo(ref, props, duration = 1) {
  // Placeholder for GSAP animation logic
  // e.g., gsap.to(ref.current, { ...props, duration })
}

export function lerp(a, b, t) {
  return a + (b - a) * t;
}
